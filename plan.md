1. **Statistical Significance Testing (`analysis/statistical_tests.py`)**: Implement proper statistical tests comparing grokking vs collapse conditions: Welch t-test on final test accuracy across seeds, Kolmogorov-Smirnov test on weight norm distributions, bootstrap confidence intervals on grokking onset step. Run multiple seeds (5-10) per condition and report p-values.
2. **Data Generation Quality (`tests/test_data.py`)**: Audit the synthetic data generation pipeline for correctness. Verify that collapse injection produces the intended distribution shift. Add unit tests checking statistical properties of generated data (marginal distribution of targets, correlation between inputs and targets).
3. **Reproducibility Package (`reproduce.py` & `STATISTICAL_ANALYSIS.md`)**: Create a reproduce.py script that runs the full experimental sweep with proper logging. Generate a results reproducibility checklist. Add deterministic seeding throughout (torch, numpy, random, CUDA).
4. **Analysis Notebook (`analysis.py`)**: Modify/expand `src/analysis.py` (or create a new `analysis/comprehensive_analysis.py` wrapper, but since the prompt says "Create analysis.py that loads all experiment results...", I'll create/update `analysis.py` in the root or `src/`) to generate: publication-quality figures (matplotlib with seaborn styling), summary statistics table in LaTeX, correlation matrix between collapse severity and all measured metrics.
5. **Testing (`tests/test_stats.py`, `tests/test_reproducibility.py`)**: Write tests for statistical functions (verify t-test on known distributions, bootstrap on known CI), data generation properties, and reproducibility (same seed produces same results).
6. **Documentation (`STATISTICAL_ANALYSIS.md`)**: Add STATISTICAL_ANALYSIS.md with methodology, results interpretation, and reproducibility guide.
